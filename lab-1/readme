git clone https://github.com/DominicMagnus/docker-containers.git


docker network ls
docker network inspect NETWORK_NAME_OR_ID

docker network create monitoring


### 1 Web server part

### 3 Monitoring part

# sudo nano prometheus.yml

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'webserver'
    static_configs:
      - targets: ['localhost:80'] # Use the appropriate port for your webserver

  - job_name: 'mysql'
    static_configs:
      - targets: ['localhost:3306'] # Use the appropriate port for your MySQL

# This command starts Prometheus and exposes its web interface on port 9090
# Make sure to replace /path/to/prometheus.yml with the actual path to your prometheus.yml configuration file on your host machine.

# docker run -d --name=prometheus --network=monitoring -p 9090:9090 prom/prometheus

docker run -d --name=prometheus --network=monitoring -p 9090:9090 -v /path/to/prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheus

# docker run -d --name=prometheus --network=monitoring -p 9090:9090 -v /home/alex/prom-container/prometheus.yml:/etc/prometheus/prometheus.yml prom/prometheus


### Open Prometheus's web interface at http://localhost:9090, go to "Targets" in the "Status" menu, and configure the targets you want to monitor. 
### You can add the IP addresses or hostnames of other containers in your monitoring network.